package Chemistry;

public class MoleculeOperation {

	Node root = null;

	void formTree(String compound)
	{
		if(root == null)
		{
     		Node node = new Node();	
			node.element = compound;
     		root = node;
		}
	}
}
